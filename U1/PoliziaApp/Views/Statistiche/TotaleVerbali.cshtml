@model IEnumerable<PoliziaApp.Models.TotaleVerbaliPerTrasgressore>

<h2>Totale Verbali per Trasgressore</h2>

<table class="table">
    <tr>
        <th>@Html.DisplayNameFor(model => model.Cognome)</th>
        <th>@Html.DisplayNameFor(model => model.Nome)</th>
        <th>@Html.DisplayNameFor(model => model.NumeroVerbali)</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.Cognome)</td>
            <td>@Html.DisplayFor(modelItem => item.Nome)</td>
            <td>@Html.DisplayFor(modelItem => item.NumeroVerbali)</td>
        </tr>
    }

</table>
