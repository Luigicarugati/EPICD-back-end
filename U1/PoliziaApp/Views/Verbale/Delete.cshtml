@model PoliziaApp.Models.Verbale

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Verbale</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>IdAnagrafica</dt>
        <dd>@Model.IdAnagrafica</dd>
        <dt>IdViolazione</dt>
        <dd>@Model.IdViolazione</dd>
        <dt>DataViolazione</dt>
        <dd>@Model.DataViolazione</dd>
        <dt>IndirizzoViolazione</dt>
        <dd>@Model.IndirizzoViolazione</dd>
        <dt>Nominativo_Agente</dt>
        <dd>@Model.Nominativo_Agente</dd>
        <dt>DataTrascrizioneVerbale</dt>
        <dd>@Model.DataTrascrizioneVerbale</dd>
        <dt>Importo</dt>
        <dd>@Model.Importo</dd>
        <dt>DecurtamentoPunti</dt>
        <dd>@Model.DecurtamentoPunti</dd>
        <dt>Attenuante</dt>
        <dd>@Model.Attenuante</dd>
        <dt>Aggravante</dt>
        <dd>@Model.Aggravante</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="IdVerbale" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
