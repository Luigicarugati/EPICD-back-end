@model IEnumerable<PoliziaMunicipaleApp.Models.TipiViolazione>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().Idviolazione)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().Descrizione)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Idviolazione)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descrizione)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Idviolazione }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Idviolazione }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Idviolazione })
                </td>
            </tr>
        }
    </tbody>
</table>

@Html.ActionLink("Create New", "Create")
